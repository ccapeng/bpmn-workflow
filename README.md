# BPMN workflow viewer and editor

In the project directory, you can run:
`npm start`

## What can do further about this workflow?
- Allow each node to attach some properties or form data.
- Create your own diagram.

